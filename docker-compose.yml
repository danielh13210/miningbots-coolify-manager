services:
  miningbots-manager:
    image: miningbots-coolify-manager
    build: .
    init: true
    container_name: miningbots-coolify-manager
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /data/mcm-data:/tmp
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - fe_host=${fe_host:?}
      - BASE_DOMAIN=${BASE_DOMAIN:?}
      - POSTGRES_CONNECT_URI=${POSTGRES_CONNECT_URI:?}
      - SECRET_KEY=${SECRET_KEY:?}
    healthcheck:
      test: ["CMD", "python3", "healthcheck.py"]
      interval: 1m30s
      timeout: 10s
      retries: 3
